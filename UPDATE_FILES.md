# Як оновлювати файли на сервері

## Спосіб 1: Через FileZilla (простий спосіб)

### Оновлення окремих файлів:

1. **Підключіться до сервера через FileZilla**
2. **Знайдіть файл, який потрібно оновлювати:**
   - Ліва сторона: ваш комп'ютер з новою версією файлу
   - Права сторона: сервер зі старою версією

3. **Перетягніть новий файл:**
   - FileZilla запитає: "Файл вже існує. Перезаписати?"
   - Натисніть "Yes" або "Yes to all" (якщо оновлюєте багато файлів)

4. **Після оновлення backend файлів:**
   ```bash
   # Підключіться до сервера через SSH або веб-термінал
   cd /home/ergoa/affiliate-tracking
   pm2 restart affiliate-tracking-api
   ```

5. **Після оновлення frontend файлів:**
   ```bash
   cd /home/ergoa/affiliate-tracking/frontend
   npm run build
   # Старі файли будуть замінені новими в папці dist/
   ```

### Оновлення всіх файлів (повний деплой):

1. **Створіть новий архів з оновленими файлами:**
   ```powershell
   cd C:\Users\ergoa\Downloads\affiliate-tracking-main\affiliate-tracking-main
   # Видаліть старий архів
   Remove-Item ..\deploy.zip -ErrorAction SilentlyContinue
   # Створіть новий
   Get-ChildItem -Exclude node_modules,.env,*.log,logs,.git,deploy.zip | Compress-Archive -DestinationPath ..\deploy.zip -Force
   ```

2. **Завантажте новий архів через FileZilla**

3. **На сервері:**
   ```bash
   cd /home/ergoa/affiliate-tracking
   # Зробіть резервну копію (опціонально)
   cp -r . ../affiliate-tracking-backup
   
   # Розпакуйте новий архів в тимчасову папку
   cd ..
   unzip deploy.zip -d affiliate-tracking-new
   
   # Скопіюйте нові файли (крім node_modules та .env)
   cp -r affiliate-tracking-new/* affiliate-tracking/
   cp -r affiliate-tracking-new/.* affiliate-tracking/ 2>/dev/null || true
   
   # Видаліть тимчасову папку
   rm -rf affiliate-tracking-new
   
   # Встановіть залежності (якщо змінився package.json)
   cd affiliate-tracking
   npm install --production
   
   # Перебілдіть frontend
   cd frontend
   npm install
   npm run build
   cd ..
   
   # Перезапустіть сервер
   pm2 restart affiliate-tracking-api
   ```

---

## Спосіб 2: Через Git (рекомендовано для регулярних оновлень)

### Налаштування Git на сервері:

1. **Створіть Git репозиторій** (GitHub, GitLab, Bitbucket)

2. **На сервері:**
   ```bash
   cd /home/ergoa
   
   # Якщо проєкт ще не підключений до Git
   cd affiliate-tracking
   git init
   git remote add origin https://github.com/your-username/your-repo.git
   git add .
   git commit -m "Initial commit"
   git push -u origin main
   
   # Якщо проєкт вже в Git
   git clone https://github.com/your-username/your-repo.git affiliate-tracking
   cd affiliate-tracking
   ```

3. **Створіть скрипт для оновлення:**
   ```bash
   nano update.sh
   ```

   **Вміст update.sh:**
   ```bash
   #!/bin/bash
   echo "🔄 Оновлення проєкту..."
   
   # Отримати останні зміни
   git pull origin main
   
   # Встановити залежності (якщо змінився package.json)
   npm install --production
   
   # Перебілдіть frontend
   cd frontend
   npm install
   npm run build
   cd ..
   
   # Перезапустити сервер
   pm2 restart affiliate-tracking-api
   
   echo "✅ Оновлення завершено!"
   ```

   ```bash
   chmod +x update.sh
   ```

4. **Оновлення проєкту:**
   ```bash
   cd /home/ergoa/affiliate-tracking
   ./update.sh
   ```

### Локальна робота:

1. **Внесіть зміни на локальному комп'ютері**
2. **Закомітьте та запуште:**
   ```bash
   git add .
   git commit -m "Опис змін"
   git push origin main
   ```

3. **На сервері виконайте:**
   ```bash
   ./update.sh
   ```

---

## Спосіб 3: Автоматичний деплой через скрипт

### Створіть скрипт для швидкого оновлення:

**На сервері створіть файл `quick-update.sh`:**
```bash
#!/bin/bash
cd /home/ergoa/affiliate-tracking

# Оновити backend залежності (якщо потрібно)
npm install --production

# Оновити frontend
cd frontend
npm install
npm run build
cd ..

# Перезапустити сервер
pm2 restart affiliate-tracking-api

echo "✅ Оновлення завершено!"
```

**Використання:**
```bash
chmod +x quick-update.sh
./quick-update.sh
```

---

## Що потрібно оновлювати після змін

### 🆕 Останні зміни (трекер v2 з verification ping):

**Обов'язково завантажте на хостинг:**

1. **`public/tracker-v2.js`** - Оновлено з verification ping
   - Трекер тепер надсилає ping кожні 5 хвилин для перевірки встановлення
   - Додано функцію `sendVerificationPing()` в публічний API

2. **`public/tracker-test.html`** - Новий файл (тестова сторінка)
   - Дозволяє користувачам перевірити роботу трекера
   - Доступна за адресою: `https://your-domain.com/tracker-test.html`

3. **`routes/track.js`** - Додано endpoint `/api/track/test`
   - Повертає детальну інформацію про статус трекера
   - Перевіряє verification ping за останні 10 хвилин

4. **`frontend/src/pages/Setup.jsx`** - Покращено відображення статусу
   - Додано детальну інформацію про статус підключення
   - Додано гарантії роботи трекера
   - Додано посилання на тестову сторінку
   - Покращено інструкції для користувачів

**Після завантаження:**
```bash
# Перебілдіть frontend
cd frontend && npm run build

# Перезапустіть сервер
pm2 restart affiliate-tracking-api
```

### Backend зміни (server.js, routes, models):
- ✅ Завантажте нові файли через FileZilla
- ✅ Виконайте: `pm2 restart affiliate-tracking-api`

### Frontend зміни (React компоненти):
- ✅ Завантажте нові файли через FileZilla
- ✅ Виконайте: `cd frontend && npm run build`
- ✅ Файли в `frontend/dist/` будуть оновлені автоматично

### Зміни в package.json:
- ✅ Завантажте новий package.json
- ✅ Виконайте: `npm install --production` (backend)
- ✅ Виконайте: `cd frontend && npm install && npm run build` (frontend)
- ✅ Перезапустіть: `pm2 restart affiliate-tracking-api`

### Зміни в .env:
- ⚠️ **НЕ завантажуйте .env через FileZilla!**
- ✅ Створіть .env вручну на сервері через SSH/термінал
- ✅ Або використайте безпечний спосіб передачі

---

## Рекомендований workflow

### Для регулярних оновлень:

1. **Використовуйте Git:**
   - Локально: зміни → commit → push
   - На сервері: `git pull` → `./update.sh`

2. **Для швидких виправлень:**
   - FileZilla: завантажте конкретний файл
   - SSH: `pm2 restart affiliate-tracking-api` (для backend)
   - SSH: `cd frontend && npm run build` (для frontend)

### Структура для зручності:

```
/home/ergoa/
├── affiliate-tracking/     # Основний проєкт
│   ├── .env                # НЕ комітити в Git!
│   ├── frontend/
│   │   ├── .env            # НЕ комітити в Git!
│   │   └── dist/           # Згенеровані файли (не комітити)
│   └── node_modules/       # Не комітити
└── affiliate-tracking-backup/  # Резервна копія (опціонально)
```

---

## Безпека

1. **Завжди робіть резервну копію перед великими оновленнями:**
   ```bash
   cp -r /home/ergoa/affiliate-tracking /home/ergoa/affiliate-tracking-backup-$(date +%Y%m%d)
   ```

2. **Перевіряйте зміни перед застосуванням:**
   ```bash
   # Подивитися, що змінилося
   git diff
   # Або для Git pull
   git fetch
   git diff HEAD origin/main
   ```

3. **Тестуйте на staging сервері** (якщо можливо) перед production

---

## Швидкі команди для оновлення

### Оновлення трекера (останні зміни):
```bash
cd /home/ergoa/affiliate-tracking

# Перебілдіть frontend (після оновлення Setup.jsx)
cd frontend && npm run build && cd ..

# Перезапустіть сервер (після оновлення routes/track.js)
pm2 restart affiliate-tracking-api
```

### Тільки backend:
```bash
cd /home/ergoa/affiliate-tracking
pm2 restart affiliate-tracking-api
```

### Тільки frontend:
```bash
cd /home/ergoa/affiliate-tracking/frontend
npm run build
```

### Все разом:
```bash
cd /home/ergoa/affiliate-tracking
npm install --production
cd frontend && npm install && npm run build && cd ..
pm2 restart affiliate-tracking-api
```

---

## 📋 Чеклист оновлення трекера на хостингу

### Крок 1: Завантажте файли через FileZilla
- [ ] `public/tracker-v2.js` (оновлено)
- [ ] `public/tracker-test.html` (новий файл)
- [ ] `routes/track.js` (додано /test endpoint)
- [ ] `frontend/src/pages/Setup.jsx` (покращено UI)

### Крок 2: На сервері через SSH
```bash
cd /home/ergoa/affiliate-tracking

# Перебілдіть frontend
cd frontend && npm run build && cd ..

# Перезапустіть сервер
pm2 restart affiliate-tracking-api
```

### Крок 3: Перевірте роботу
- [ ] Відкрийте `https://your-domain.com/tracker-test.html`
- [ ] Перевірте статус на сторінці Setup → "Мої сайти"
- [ ] Перевірте логи: `pm2 logs affiliate-tracking-api`

---

## Потрібна допомога?

Якщо виникли проблеми після оновлення:
1. Перевірте логи: `pm2 logs affiliate-tracking-api`
2. Перевірте помилки: `pm2 logs affiliate-tracking-api --err`
3. Відкотите зміни: використайте резервну копію
4. Перевірте тестову сторінку: `https://your-domain.com/tracker-test.html`
