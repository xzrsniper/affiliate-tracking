# Google OAuth Client ID (локальна збірка / dev)
# На продакшні можна задати тільки на бекенді: GOOGLE_CLIENT_ID_PUBLIC=... у .env у корені проєкту — фронт підхопить з /api/config/public
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
