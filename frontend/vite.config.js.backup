import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true,
    strictPort: false,
    hmr: {
      // Use WebSocket for HMR instead of eval() when possible
      protocol: 'ws',
    },
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        secure: false,
      },
      '/track': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        secure: false,
      },
      '/pixel.js': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        secure: false,
      },
    },
  },
  optimizeDeps: {
    force: true,
    esbuildOptions: {
      // Fix for Windows EPERM errors
      target: 'esnext',
    },
  },
  build: {
    // Disable eval in production builds
    target: 'esnext',
    minify: false, // Temporarily disable minification to avoid esbuild issues
  },
  esbuild: {
    // Fix for Windows permission issues
    target: 'esnext',
  },
})
