# 🚀 Проста інструкція з встановлення трекера

## Для сайтів БЕЗ бекенду (Netlify, Vercel, GitHub Pages)

### Крок 1: Знайдіть ваш Site ID
1. Відкрийте кабінет Lehko Track
2. Перейдіть на вкладку **"Мої сайти"**
3. Знайдіть ваш сайт в таблиці
4. В колонці **"ID сайту"** буде число (наприклад: `6`)
5. **Скопіюйте це число**

### Крок 2: Вставте код в GTM
1. Відкрийте Google Tag Manager
2. Створіть новий тег: **Tags** → **New** → **Custom HTML**
3. Назвіть тег: `LehkoTrack`
4. Вставте цей код (замініть `6` на ваш Site ID):
   ```html
   <script src="https://lehko.space/pixel.js?id=6"></script>
   ```
5. Тригер: **All Pages**
6. Збережіть та опублікуйте (Submit → Publish)

### Крок 3: Перевірте
1. Відкрийте ваш сайт у браузері
2. Натисніть **F12** → вкладка **Console**
3. Має з'явитися: `[Lehko Track] ✅ Tracker initialized`
4. Має з'явитися: `[Lehko Track] 📡 Sending verification ping`
5. Має з'явитися: `[Lehko Track] ✅ Verification ping successful!`

### Крок 4: Перевірте в кабінеті
1. Зачекайте 1-2 хвилини після відкриття сайту
2. В кабінеті → **"Мої сайти"** → натисніть **"Перевірити зараз"**
3. Статус має стати **"Підключено"**

## ❌ Якщо не працює

### Перевірте в Console (F12):
- Якщо бачите помилку `404` → неправильний API URL
- Якщо бачите помилку `CORS` → проблема з налаштуваннями сервера
- Якщо бачите `Verification ping failed` → перевірте API URL

### Перевірте в Network (F12 → Network):
- Шукайте запит до `pixel.js` → має бути статус `200`
- Шукайте запит до `/api/track/verify` → має бути статус `200`

### Перевірте код в GTM:
- Має бути: `<script src="https://lehko.space/pixel.js?id=6"></script>`
- НЕ має бути: `app.netlify.com` або `localhost`
- Переконайтеся що тег опублікований (Submit → Publish)

## ✅ Правильний код для GTM

```html
<script src="https://lehko.space/pixel.js?id=6"></script>
```

Де:
- `lehko.space` — загальний API сервер (для всіх сайтів без бекенду)
- `6` — ваш Site ID (знайдіть в кабінеті)

## 📞 Якщо все ще не працює

1. Перевірте що тег опублікований в GTM
2. Очистіть кеш браузера (Ctrl+Shift+Delete)
3. Відкрийте сайт у режимі інкогніто
4. Перевірте Console та Network вкладки
5. Надішліть скріншоти помилок
