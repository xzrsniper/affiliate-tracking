# Налаштування трекера для сайту на Netlify

## 🔍 Розуміння архітектури

Коли ваш сайт (`shopin.pp.ua`) хоститься на **Netlify**, важливо розуміти різницю:

### Фронтенд (ваш сайт на Netlify)
- **Домен:** `shopin.pp.ua` або `your-site.netlify.app`
- **Що це:** Статичні файли (HTML, CSS, JS) — ваш магазин
- **Де:** Netlify

### Бекенд API (сервер Lehko Track)
- **Домен:** `https://lehko.space` або ваш API домен
- **Що це:** Сервер, який обробляє tracking запити
- **Де:** Окремий сервер (Render, Railway, VPS, тощо)

## ⚠️ Важливо

**Трекер (`pixel.js`) завантажується з API сервера, НЕ з Netlify!**

Коли ви вставляєте код:
```html
<script src="https://lehko.space/pixel.js?id=6"></script>
```

Браузер завантажує `pixel.js` з `lehko.space` (ваш API сервер), а не з `shopin.pp.ua` (Netlify).

## ✅ Правильна конфігурація

### Варіант 1: API на окремому сервері (рекомендовано)

Якщо у вас є API сервер (Render, Railway, VPS, тощо):

1. **В GTM тег має бути:**
   ```html
   <script src="https://lehko.space/pixel.js?id=6"></script>
   ```
   (Замініть `lehko.space` на ваш API домен)

2. **Переконайтеся що:**
   - API сервер доступний з інтернету
   - API сервер має HTTPS сертифікат
   - Файл `pixel.js` доступний за URL `https://your-api-domain.com/pixel.js`

### Варіант 2: Якщо API на тому ж домені (через Netlify Functions)

Якщо ви хочете використовувати `shopin.pp.ua` як API (складніше):

1. **Створіть Netlify Function** для проксування API запитів
2. **Налаштуйте rewrites** в `netlify.toml`
3. **Використовуйте** `https://shopin.pp.ua/pixel.js?id=6` в GTM

**Але це складніше і потребує налаштування бекенду на Netlify.**

## 📋 Покрокова інструкція для GTM

1. **Відкрийте GTM** → Tags → знайдіть тег "LehkoTrack"

2. **Перевірте код:**
   ```html
   <script src="https://lehko.space/pixel.js?id=6"></script>
   ```
   - `lehko.space` — це ваш API домен (де розгорнуто бекенд)
   - `6` — це ваш Site ID (знайдіть в кабінеті → "Мої сайти")

3. **НЕ використовуйте:**
   - ❌ `https://app.netlify.com/` — це панель управління
   - ❌ `https://shopin.pp.ua` — це фронтенд (якщо API не налаштовано)
   - ❌ `https://your-site.netlify.app` — це фронтенд

4. **Опублікуйте тег** (Submit → Publish)

5. **Перевірте:**
   - Відкрийте `https://shopin.pp.ua` у браузері
   - F12 → Console → має з'явитися `[Lehko Track] ✅ Tracker initialized`
   - F12 → Network → шукайте `pixel.js` → статус має бути 200

## 🎯 Де знайти правильний API URL?

1. **Якщо використовуєте `lehko.space`:**
   - API URL: `https://lehko.space`
   - Код: `<script src="https://lehko.space/pixel.js?id=6"></script>`

2. **Якщо у вас свій API сервер:**
   - Знайдіть домен де розгорнуто бекенд Lehko Track
   - Це може бути Render, Railway, VPS, тощо
   - Використовуйте цей домен як API URL

3. **Якщо не знаєте де API:**
   - Перевірте де ви розгортали бекенд Lehko Track
   - Перевірте налаштування в кабінеті
   - Зв'яжіться з адміністратором системи

## ❓ Часті питання

**Q: Чому не можна використати `shopin.pp.ua` як API URL?**
A: Тому що `shopin.pp.ua` на Netlify — це тільки статичні файли. API сервер має бути окремо.

**Q: Як перевірити чи правильний API URL?**
A: Відкрийте в браузері `https://your-api-domain.com/pixel.js` — має завантажитися JavaScript файл.

**Q: Чи можна використати Netlify Functions для API?**
A: Так, але це потребує налаштування бекенду на Netlify. Простіше використати окремий API сервер.
