# 🚀 Швидкий старт - Встановлення трекера

## Для сайтів БЕЗ бекенду (Netlify, Vercel, GitHub Pages)

### Крок 1: Знайдіть Site ID
1. Відкрийте кабінет → вкладка **"Мої сайти"**
2. Знайдіть ваш сайт в таблиці
3. Скопіюйте число з колонки **"ID сайту"** (наприклад: `6`)

### Крок 2: Вставте код в GTM
1. Відкрийте Google Tag Manager
2. Створіть тег: **Tags** → **New** → **Custom HTML**
3. Назва: `LehkoTrack`
4. Вставте код (замініть `6` на ваш Site ID):
   ```html
   <script>
   (function() {
     var script = document.createElement('script');
     script.src = 'https://lehko.space/pixel.js?id=6';
     script.async = true;
     (document.head || document.getElementsByTagName('head')[0]).appendChild(script);
   })();
   </script>
   ```
5. Тригер: **All Pages**
6. **Save** → **Submit** → **Publish**

**⚠️ Важливо:** Для GTM використовуйте програмне завантаження (код вище), а не простий `<script src="...">` тег!

### Крок 3: Перевірте
1. Відкрийте ваш сайт
2. Натисніть **F12** → **Console**
3. Має з'явитися:
   - `[Lehko Track] ✅ Tracker initialized`
   - `[Lehko Track] 📡 Sending verification ping`
   - `[Lehko Track] ✅ Verification ping successful!`
4. Зачекайте 1-2 хвилини
5. В кабінеті → **"Мої сайти"** → **"Перевірити зараз"**
6. Статус має стати **"Підключено"**

## ❌ Якщо не працює

### Перевірте Console (F12):
- ❌ `404` → неправильний API URL
- ❌ `CORS` → проблема з сервером
- ❌ `Verification ping failed` → перевірте API URL

### Перевірте Network (F12 → Network):
- Шукайте `pixel.js` → має бути статус `200`
- Шукайте `/api/track/verify` → має бути статус `200`

### Перевірте код в GTM:
- ✅ Має бути: `https://lehko.space/pixel.js?id=6`
- ❌ НЕ має бути: `app.netlify.com` або `localhost`
- ✅ Переконайтеся що тег опублікований

## ✅ Правильний код

### Для GTM (рекомендовано):
```html
<script>
(function() {
  var script = document.createElement('script');
  script.src = 'https://lehko.space/pixel.js?id=YOUR_SITE_ID';
  script.async = true;
  (document.head || document.getElementsByTagName('head')[0]).appendChild(script);
})();
</script>
```

### Для прямого встановлення в HTML:
```html
<script src="https://lehko.space/pixel.js?id=YOUR_SITE_ID"></script>
```

Де:
- `lehko.space` — загальний API сервер (для всіх сайтів без бекенду)
- `YOUR_SITE_ID` — ваш Site ID з кабінету (наприклад: `6`)

## 📞 Допомога

Якщо все ще не працює:
1. Перевірте що тег опублікований в GTM
2. Очистіть кеш браузера (Ctrl+Shift+Delete)
3. Відкрийте сайт у режимі інкогніто
4. Перевірте Console та Network
5. Надішліть скріншоти помилок
