# Як змінити ADMIN_EMAIL на сервері

## Спосіб 1: Через SSH/Термінал (рекомендовано)

### Крок 1: Підключіться до сервера

```bash
# Через SSH
ssh ergoa@your-server-ip

# Або через веб-термінал вашого хостингу
```

### Крок 2: Перейдіть в директорію проєкту

```bash
cd /home/ergoa/affiliate-tracking
```

### Крок 3: Відредагуйте .env файл

```bash
# Відкрийте .env файл в редакторі
nano .env
```

### Крок 4: Знайдіть та змініть ADMIN_EMAIL

У файлі знайдіть рядок:
```env
ADMIN_EMAIL=admin@example.com
```

Змініть на потрібний email (наприклад, на email вашого адміністратора):
```env
ADMIN_EMAIL=admin@lehko.space
```

### Крок 5: Збережіть файл

- **У nano:** Натисніть `Ctrl+O` (зберегти), потім `Enter`, потім `Ctrl+X` (вийти)
- **У vim:** Натисніть `Esc`, потім `:wq` та `Enter`

### Крок 6: Перезапустіть сервер

```bash
pm2 restart affiliate-tracking-api
```

### Крок 7: Перевірте, що зміни застосувалися

```bash
# Запустіть діагностику
npm run diagnose-admin
```

Або перевірте вручну:
```bash
# Подивитися значення ADMIN_EMAIL
grep ADMIN_EMAIL .env
```

---

## Спосіб 2: Через FileZilla (якщо немає SSH)

⚠️ **Увага:** Згідно з документацією, .env файл не рекомендується завантажувати через FileZilla з міркувань безпеки. Але якщо SSH недоступний, можна використати цей спосіб.

### Крок 1: Відкрийте FileZilla та підключіться до сервера

### Крок 2: Перейдіть в директорію проєкту

На сервері (права сторона) перейдіть в:
```
/home/ergoa/affiliate-tracking
```

### Крок 3: Завантажте .env файл на локальний комп'ютер

1. Знайдіть файл `.env` на сервері
2. Клацніть правою кнопкою → "Завантажити"
3. Збережіть на локальний комп'ютер

### Крок 4: Відредагуйте файл локально

Відкрийте завантажений `.env` файл у текстовому редакторі та змініть:
```env
ADMIN_EMAIL=admin@lehko.space
```

### Крок 5: Завантажте оновлений файл назад на сервер

1. Перетягніть оновлений `.env` файл з локального комп'ютера на сервер
2. FileZilla запитає: "Файл вже існує. Перезаписати?" → Натисніть "Yes"

### Крок 6: Перезапустіть сервер через SSH або веб-термінал

```bash
cd /home/ergoa/affiliate-tracking
pm2 restart affiliate-tracking-api
```

---

## Спосіб 3: Через команду sed (швидкий спосіб)

Якщо ви вже підключені через SSH, можна змінити однією командою:

```bash
cd /home/ergoa/affiliate-tracking

# Зробити резервну копію
cp .env .env.backup

# Змінити ADMIN_EMAIL (замініть на потрібний email)
sed -i 's/^ADMIN_EMAIL=.*/ADMIN_EMAIL=admin@lehko.space/' .env

# Перевірити зміни
grep ADMIN_EMAIL .env

# Перезапустити сервер
pm2 restart affiliate-tracking-api
```

---

## Перевірка після зміни

### 1. Перевірте змінну середовища

```bash
cd /home/ergoa/affiliate-tracking
npm run diagnose-admin
```

Має показати:
```
📧 ADMIN_EMAIL змінна середовища:
   ✅ Встановлено: admin@lehko.space
```

### 2. Перевірте, що адміністратор відповідає ADMIN_EMAIL

```bash
npm run diagnose-admin
```

Має показати:
```
Email відповідає ADMIN_EMAIL: Так ✅
```

### 3. Перевірте логи сервера

```bash
pm2 logs affiliate-tracking-api --lines 50
```

Переконайтеся, що немає помилок.

---

## Важливо!

1. **Після зміни ADMIN_EMAIL обов'язково перезапустіть сервер:**
   ```bash
   pm2 restart affiliate-tracking-api
   ```

2. **Переконайтеся, що email адміністратора відповідає ADMIN_EMAIL:**
   - Якщо ні, або змініть ADMIN_EMAIL на email адміністратора
   - Або створіть нового адміністратора з email, що відповідає ADMIN_EMAIL:
     ```bash
     npm run create-admin admin@lehko.space your-password
     ```

3. **Зробіть резервну копію .env перед змінами:**
   ```bash
   cp .env .env.backup-$(date +%Y%m%d)
   ```

---

## Приклад повної послідовності дій

```bash
# 1. Підключитися до сервера
ssh ergoa@your-server-ip

# 2. Перейти в директорію проєкту
cd /home/ergoa/affiliate-tracking

# 3. Зробити резервну копію
cp .env .env.backup

# 4. Відредагувати .env
nano .env
# Знайти ADMIN_EMAIL=admin@example.com
# Змінити на ADMIN_EMAIL=admin@lehko.space
# Зберегти: Ctrl+O, Enter, Ctrl+X

# 5. Перевірити зміни
grep ADMIN_EMAIL .env

# 6. Перезапустити сервер
pm2 restart affiliate-tracking-api

# 7. Перевірити діагностику
npm run diagnose-admin

# 8. Перевірити логи
pm2 logs affiliate-tracking-api --lines 20
```

---

## Якщо щось пішло не так

### Відкотити зміни:

```bash
cd /home/ergoa/affiliate-tracking
cp .env.backup .env
pm2 restart affiliate-tracking-api
```

### Перевірити поточний стан:

```bash
cd /home/ergoa/affiliate-tracking
npm run diagnose-admin
npm run check-admin
```

### Перевірити логи помилок:

```bash
pm2 logs affiliate-tracking-api --err
```
