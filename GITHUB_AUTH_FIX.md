# 🔐 Виправлення помилки аутентифікації GitHub

GitHub більше не підтримує паролі для HTTPS (з серпня 2021). Потрібно використовувати **Personal Access Token** або **SSH ключі**.

## Варіант 1: Personal Access Token (PAT) - Найпростіше

### Крок 1: Створіть Personal Access Token

1. Перейдіть на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   Або прямо: https://github.com/settings/tokens

2. Натисніть "Generate new token" → "Generate new token (classic)"

3. Назвіть токен (наприклад: "Affiliate Tracking Deploy")

4. Оберіть права доступу:
   - ✅ `repo` (повний доступ до репозиторіїв)
   - ✅ `workflow` (якщо використовуєте GitHub Actions)

5. Натисніть "Generate token"

6. **ВАЖЛИВО**: Скопіюйте токен одразу (він показується тільки один раз!)
   Токен виглядає так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Крок 2: Використання токену

**При push GitHub запитає пароль:**
- Username: ваш GitHub username
- Password: **вставте токен** (НЕ пароль!)

```bash
git push -u origin main
# Username: xzrsniper
# Password: ghp_ваш_токен_тут
```

### Крок 3: Збереження токену (щоб не вводити кожного разу)

**macOS/Linux:**
```bash
# Збереження в Git credential helper
git config --global credential.helper store

# Або для одного репозиторію
git config credential.helper store
```

Після цього при першому push введіть токен, і він збережеться.

**Або через URL (менш безпечно, але простіше):**
```bash
# Замініть токен в URL
git remote set-url origin https://ghp_ВАШ_ТОКЕН@github.com/xzrsniper/affiliate-tracking.git

# Потім push
git push -u origin main
```

---

## Варіант 2: SSH ключі (рекомендовано для довгострокового використання)

### Крок 1: Створення SSH ключа

```bash
# Перевірте, чи вже є SSH ключ
ls -al ~/.ssh

# Якщо немає, створіть новий
ssh-keygen -t ed25519 -C "your_email@example.com"

# Натисніть Enter для збереження в стандартному місці
# Введіть пароль (або залиште порожнім)
```

### Крок 2: Додайте ключ до ssh-agent

```bash
# Запустіть ssh-agent
eval "$(ssh-agent -s)"

# Додайте ключ
ssh-add ~/.ssh/id_ed25519
```

### Крок 3: Додайте публічний ключ на GitHub

```bash
# Скопіюйте публічний ключ
cat ~/.ssh/id_ed25519.pub
# Або на macOS:
pbcopy < ~/.ssh/id_ed25519.pub
```

1. Перейдіть на GitHub → Settings → SSH and GPG keys
   Або: https://github.com/settings/keys

2. Натисніть "New SSH key"

3. Назвіть ключ (наприклад: "MacBook Pro")

4. Вставте скопійований ключ

5. Натисніть "Add SSH key"

### Крок 4: Змініть remote на SSH

```bash
# Змініть URL на SSH
git remote set-url origin git@github.com:xzrsniper/affiliate-tracking.git

# Перевірте
git remote -v

# Тестуйте підключення
ssh -T git@github.com
# Має показати: Hi xzrsniper! You've successfully authenticated...

# Push
git push -u origin main
```

---

## Варіант 3: GitHub CLI (gh) - Альтернатива

```bash
# Встановлення GitHub CLI
brew install gh  # macOS
# або
sudo apt install gh  # Linux

# Авторизація
gh auth login

# Виберіть GitHub.com → HTTPS → Login with a web browser
# Або введіть токен вручну
```

---

## Швидке виправлення (якщо вже є токен)

```bash
# Змініть remote з токеном в URL
git remote set-url origin https://ghp_ВАШ_ТОКЕН@github.com/xzrsniper/affiliate-tracking.git

# Push
git push -u origin main
```

---

## Troubleshooting

### "Permission denied (publickey)"
- Перевірте, чи SSH ключ додано на GitHub
- Перевірте підключення: `ssh -T git@github.com`

### "Authentication failed" з токеном
- Переконайтеся, що токен правильний (починається з `ghp_`)
- Переконайтеся, що токен має права `repo`
- Токен не застарів (може бути видалений)

### "Repository not found"
- Перевірте, чи репозиторій існує на GitHub
- Перевірте, чи у вас є права доступу до репозиторію

---

## Рекомендації

1. **Для продакшн**: Використовуйте SSH ключі (безпечніше)
2. **Для швидкого деплою**: Personal Access Token
3. **Для команди**: GitHub CLI або SSH

**Найпростіше зараз**: Створіть Personal Access Token і використайте його як пароль при push.

