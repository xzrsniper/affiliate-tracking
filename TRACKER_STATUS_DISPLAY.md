# 📊 Відображення статусу трекера

## Що було додано

1. ✅ **Модель TrackerVerification** - зберігає verification pings від трекера
2. ✅ **Покращена перевірка** - використовує verification pings як основний метод
3. ✅ **GTM підтримка** - трекер працює через Google Tag Manager
4. ✅ **Автоматична перевірка** - система автоматично визначає наявність трекера

---

## Як працює перевірка

### Метод 1: Verification Pings (основний)

Трекер відправляє verification ping кожні 5 хвилин на:
```
https://lehko.space/api/track/verify?code=YOUR_CODE&domain=your-site.com&version=gtm
```

Система перевіряє, чи був ping в останні 10 хвилин:
- ✅ Якщо так - трекер встановлено
- ❌ Якщо ні - перевіряє через HTML scraping

### Метод 2: HTML Scraping (резервний)

Якщо verification ping не знайдено, система:
1. Завантажує HTML сторінки сайту
2. Шукає індикатори трекера (tracker.js, TRACKER_CONFIG, etc.)
3. Визначає статус на основі знайдених індикаторів

---

## Відображення статусу в панелі

### В розділі "Сайти"

Статус відображається як:
- ✅ **Підключено** - трекер знайдено через verification ping або HTML scraping
- ❌ **Не підключено** - трекер не знайдено

### Автоматична перевірка

Статус автоматично оновлюється:
- При створенні сайту (якщо вказано domain)
- При зміні домену
- При натисканні кнопки "Перевірити підключення"

---

## Створення таблиці для verification pings

На сервері виконайте:

```bash
cd ~/affiliate-tracking
node scripts/add-tracker-verification-table.js
```

Це створить таблицу `tracker_verifications` для зберігання verification pings.

---

## Перевірка статусу

### В панелі управління

1. Перейдіть в "Сайти" (Websites)
2. Додайте ваш домен
3. Натисніть "Перевірити підключення"
4. Статус оновиться автоматично

### Через API

```bash
curl https://lehko.space/api/websites/YOUR_WEBSITE_ID/check \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## Підтримка GTM

Трекер працює через Google Tag Manager:
- Версія: `gtm` (відображається в verification ping)
- Автоматична перевірка працює так само
- Verification ping відправляється кожні 5 хвилин

---

## Важливо!

- ✅ Verification ping - найнадійніший метод перевірки
- ✅ Трекер відправляє ping кожні 5 хвилин
- ✅ Система перевіряє pings за останні 10 хвилин
- ✅ HTML scraping використовується як резервний метод
