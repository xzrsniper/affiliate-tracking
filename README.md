# Affiliate Tracking System - Система Відслідковування Афіліатів

## 📋 Про проект

**Affiliate Tracking System** - це проста та потужна система для відслідковування афіліат-посилань. Система дозволяє:

- ✅ Генерувати унікальні tracking посилання
- ✅ Відслідковувати кліки та унікальних відвідувачів
- ✅ Відслідковувати конверсії (покупки)
- ✅ Аналізувати статистику в реальному часі
- ✅ Управляти користувачами через адмін-панель

**Tech Stack:**
- Backend: Node.js + Express + MySQL (Sequelize)
- Frontend: React + Vite + Tailwind CSS
- Authentication: JWT

---

## 🚀 Швидкий старт

### Вимоги

- Node.js (v18 або вище)
- MySQL (локальна або віддалена база даних)
- npm або yarn

### Крок 1: Встановлення залежностей

```bash
# Встановити залежності для backend
npm install

# Встановити залежності для frontend
cd frontend
npm install
cd ..
```

### Крок 2: Налаштування бази даних

**2.1 Створіть базу даних MySQL:**

```sql
CREATE DATABASE affiliate_tracking;
```

**2.2 Налаштуйте змінні оточення:**

Створіть файл `.env` в кореневій директорії проєкту:

```bash
cp .env.example .env
```

Відредагуйте `.env` файл:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=affiliate_tracking
DB_USER=root
DB_PASSWORD=ваш_пароль_mysql

# JWT Secret (змініть на випадковий рядок!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=3000
NODE_ENV=development
```

**2.3 Ініціалізуйте базу даних:**

```bash
npm run db:init
```

Це створить всі необхідні таблиці автоматично.

**2.4 Створіть першого адміністратора:**

```bash
npm run create-admin admin@example.com ваш_пароль
```

### Крок 3: Запуск серверів

**Запустіть backend сервер:**

```bash
npm run dev
```

Backend буде доступний на: `http://localhost:3000`

**У новому терміналі запустіть frontend:**

```bash
cd frontend
npm run dev
```

Frontend буде доступний на: `http://localhost:5173`

### Крок 4: Перший вхід

1. Відкрийте браузер: `http://localhost:5173`
2. Увійдіть зі створеними обліковими даними адміністратора
3. Почніть створювати tracking посилання!

---

## 🔗 Інтеграція на ваш сайт

### Крок 1: Встановіть tracking script

Додайте наступний код в секцію `<head>` вашого сайту (перед закриваючим тегом `</head>`):

```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'http://your-backend-url/api/track',  // ← ЗМІНІТЬ на URL вашого backend
        DEBUG: false  // Встановіть true для відладки
    };
</script>
<script src="http://your-backend-url/tracker.js"></script>
```

**Важливо:** Замініть `http://your-backend-url` на реальний URL вашого backend сервера.

**Приклад для production:**
```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'https://api.yourapp.com/api/track',
        DEBUG: false
    };
</script>
<script src="https://api.yourapp.com/tracker.js"></script>
```

### Крок 2: Налаштуйте ключове слово для конверсій

За замовчуванням система відслідковує конверсії на сторінках, що містять слово `"order"` в URL.

**Якщо ваша сторінка замовлення має інший формат URL**, налаштуйте це:

```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'https://api.yourapp.com/api/track',
        CONVERSION_KEYWORD: 'thank-you',  // ← Змініть на ваше ключове слово
        DEBUG: false
    };
</script>
<script src="https://api.yourapp.com/tracker.js"></script>
```

**Доступні варіанти ключових слів:**
- `'order'` (за замовчуванням)
- `'thank-you'` або `'thankyou'`
- `'success'`
- `'confirmation'`
- `'complete'`
- `'purchase'`

Або використовуйте масив для декількох варіантів:

```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'https://api.yourapp.com/api/track',
        CONVERSION_KEYWORDS: ['order', 'thank-you', 'success'],  // Кілька варіантів
        DEBUG: false
    };
</script>
```

### Крок 3: Перевірте роботу

1. Створіть tracking link в Dashboard
2. Відкрийте tracking URL: `http://your-backend/track/YOUR_CODE`
3. Перевірте, що відбувся редирект на ваш сайт з параметром `?ref=YOUR_CODE`
4. Зробіть тестову покупку і перейдіть на сторінку замовлення
5. Перевірте в Dashboard - має збільшитися кількість конверсій

---

## 👨‍💼 Адмін-панель

### Доступ до адмін-панелі

1. Увійдіть в систему як суперадмін
2. У меню зліва оберіть "Admin"
3. Адмін-панель дозволяє:
   - Переглядати всіх користувачів
   - Шукати користувачів за email
   - Переглядати статистику будь-якого користувача
   - Змінювати ліміт посилань для користувачів
   - Банити/розбанити користувачів

### Створення додаткових адмінів

Щоб створити ще одного адміністратора:

```bash
npm run create-admin новий_адмін@example.com пароль123
```

**Примітка:** Перший адмін створюється через скрипт `create-admin`. Додаткових адмінів можна створити також через цей скрипт, або вручну встановивши `role = 'super_admin'` в базі даних.

---

## 📊 Як працює система

### Процес відслідковування:

1. **Користувач створює tracking link** в Dashboard
   - Система генерує унікальний код (наприклад: `ABC123`)
   - Створюється tracking URL: `http://your-backend/track/ABC123`

2. **Користувач ділиться tracking URL** на блозі/соцмережах

3. **Хтось клікає на tracking URL:**
   - Backend записує клік в базу даних
   - Робить редирект на цільовий сайт з параметром `?ref=ABC123`

4. **На цільовому сайті:**
   - `tracker.js` захоплює параметр `ref=ABC123`
   - Зберігає його в localStorage + cookie (для підтримки subdomain)
   - Відслідковує кліки

5. **Якщо відвідувач робить покупку:**
   - Переходить на сторінку замовлення (містить ключове слово, наприклад `order`)
   - `tracker.js` виявляє конверсію
   - Відправляє дані про покупку на backend
   - В Dashboard відображається конверсія та дохід

---

## 🛠️ Додаткові налаштування

### Налаштування для subdomain

Якщо ваш сайт використовує subdomain (наприклад, `shop.example.com` → `checkout.example.com`), tracking script автоматично використовує cookies для обміну даними між subdomain.

Система працює автоматично, але ви можете вказати домен вручну:

```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'https://api.yourapp.com/api/track',
        COOKIE_DOMAIN: '.example.com',  // ← Замініть на ваш домен
        DEBUG: false
    };
</script>
```

### Захист від дубльованих конверсій

Система автоматично захищає від дублювання конверсій при оновленні сторінки (F5). Кожна конверсія відслідковується лише один раз.

### Відладка

Для відлагодження встановіть `DEBUG: true`:

```html
<script>
    window.TRACKER_CONFIG = {
        BASE_URL: 'https://api.yourapp.com/api/track',
        DEBUG: true  // ← Показувати помилки в консолі браузера
    };
</script>
```

Потім відкрийте консоль браузера (F12) для перегляду логів.

---

## 🔒 Безпека

### Production налаштування

Перед деплоєм на production:

1. **Змініть JWT_SECRET** в `.env` на випадковий рядок:
   ```env
   JWT_SECRET=your-very-long-random-string-here
   ```

2. **Налаштуйте CORS** для ваших доменів:
   - В `server.js` налаштуйте `origin` в CORS конфігурації
   - Або використовуйте змінну оточення

3. **Використовуйте HTTPS** в production

4. **Обмежте доступ до адмін-панелі** (наприклад, через VPN або IP whitelist)

---

## 📁 Структура проєкту

```
DashCurs/
├── config/          # Конфігурація бази даних
├── models/          # Sequelize моделі (User, Link, Click, Conversion)
├── routes/          # Express routes (auth, links, track, admin)
├── middleware/      # Middleware (authentication)
├── utils/           # Утиліти (JWT, fingerprint, code generator)
├── public/          # Статичні файли (tracker.js)
├── scripts/         # Скрипти (init DB, create admin)
├── frontend/        # React frontend
│   ├── src/
│   │   ├── pages/   # Сторінки (Login, Dashboard, Admin)
│   │   ├── components/
│   │   └── context/ # AuthContext
└── server.js        # Головний сервер
```

---

## 🐛 Вирішення проблем

### Проблема: База даних не підключається

**Рішення:**
1. Перевірте, чи запущений MySQL
2. Перевірте правильність даних в `.env` файлі
3. Перевірте, чи існує база даних: `SHOW DATABASES;`

### Проблема: Tracking links не працюють

**Рішення:**
1. Перевірте, чи backend сервер запущений
2. Перевірте CORS налаштування
3. Перевірте консоль браузера на помилки (з `DEBUG: true`)

### Проблема: Конверсії не відслідковуються

**Рішення:**
1. Перевірте, чи встановлено `tracker.js` на цільовому сайті
2. Перевірте, чи URL сторінки замовлення містить ключове слово
3. Перевірте, чи є параметр `?ref=CODE` в URL після редиректу
4. Увімкніть `DEBUG: true` для перегляду логів

### Проблема: Frontend не відображається

**Рішення:**
1. Перевірте, чи встановлені всі залежності: `npm install` в папці `frontend`
2. Перевірте, чи працює backend сервер
3. Перевірте налаштування `VITE_API_URL` в `frontend/.env`

---

## 📚 Корисні команди

```bash
# Ініціалізація бази даних
npm run db:init

# Скинути базу даних (видалить всі дані!)
npm run db:reset

# Створити адміністратора
npm run create-admin <email> <password>

# Запустити backend (development)
npm run dev

# Запустити backend (production)
npm start

# Запустити frontend
cd frontend && npm run dev

# Збілдити frontend для production
cd frontend && npm run build
```

---

## 📞 Підтримка

Якщо виникли питання або проблеми:

1. Перевірте документацію в цьому README
2. Перегляньте логи сервера та консоль браузера
3. Увімкніть `DEBUG: true` для детальних логів

---

## 📝 Ліцензія

ISC

---

## 🎯 Швидкий чеклист для запуску

- [ ] Встановлено Node.js та MySQL
- [ ] Встановлено залежності (`npm install` в корені та `frontend/`)
- [ ] Створено базу даних MySQL
- [ ] Налаштовано `.env` файл з правильними даними
- [ ] Ініціалізовано базу даних (`npm run db:init`)
- [ ] Створено першого адміна (`npm run create-admin`)
- [ ] Запущено backend сервер
- [ ] Запущено frontend сервер
- [ ] Увійшли в систему як адмін
- [ ] Встановлено `tracker.js` на цільовому сайті

**Вітаємо! Система готова до використання! 🎉**
