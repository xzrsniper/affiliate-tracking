# 🔧 Виправлення помилки PM2 "No script path"

## Проблема

PM2 не може знайти скрипт або конфігурація неправильна.

## Рішення

### Варіант 1: Використати правильну конфігурацію

Файл `ecosystem.config.js` вже виправлено. Тепер запустіть:

```bash
# Переконайтеся, що ви в правильній директорії
cd /var/www/affiliate-tracking

# Перевірте, чи файли існують
ls -la server.js ecosystem.config.js

# Запустіть PM2
pm2 start ecosystem.config.js

# Або безпосередньо
pm2 start server.js --name affiliate-tracking-api
```

### Варіант 2: Запуск без конфігурації

```bash
cd /var/www/affiliate-tracking

# Створіть директорію для логів
mkdir -p logs

# Запустіть напряму
pm2 start server.js --name affiliate-tracking-api --env production

# Збережіть конфігурацію
pm2 save
```

### Варіант 3: Перевірка шляхів

```bash
# Перевірте поточну директорію
pwd

# Має бути: /var/www/affiliate-tracking

# Перевірте файли
ls -la server.js
ls -la ecosystem.config.js

# Якщо файлів немає, перевірте, чи код завантажено
```

## Перевірка після запуску

```bash
# Статус PM2
pm2 status

# Логи
pm2 logs affiliate-tracking-api

# Детальна інформація
pm2 describe affiliate-tracking-api
```

## Якщо все ще не працює

### Перевірка помилок

```bash
# Перевірте логи
pm2 logs affiliate-tracking-api --lines 50

# Перевірте, чи Node.js встановлений
node --version

# Перевірте, чи .env файл існує
ls -la .env

# Перевірте, чи база даних налаштована
cat .env | grep DB_
```

### Перезапуск PM2

```bash
# Зупинити всі процеси
pm2 stop all
pm2 delete all

# Запустити заново
pm2 start ecosystem.config.js

# Або
pm2 start server.js --name affiliate-tracking-api
```

## Автозапуск при перезавантаженні сервера

```bash
# Збережіть поточну конфігурацію
pm2 save

# Налаштуйте автозапуск
pm2 startup

# Виконайте команду, яку виведе PM2 (зазвичай з sudo)
```

## Troubleshooting

### "Cannot find module"
- Перевірте, чи встановлені залежності: `npm install --production`
- Перевірте, чи ви в правильній директорії

### "Port 3000 already in use"
- Зупиніть інший процес: `lsof -ti:3000 | xargs kill`
- Або змініть PORT в .env

### "Database connection failed"
- Перевірте .env файл
- Перевірте, чи MySQL запущений: `sudo systemctl status mysql`
- Перевірте підключення: `mysql -u affiliate_user -p`

