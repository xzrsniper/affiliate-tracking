# 🚀 Швидкий запуск ngrok (2 тунелі одночасно)

## Крок 1: Запустіть ngrok з конфігурацією

```bash
cd /Users/ivanivanuk/Documents/DashCurs
ngrok start --all --config=ngrok.yml
```

## Крок 2: Отримайте URLs

Відкрийте http://localhost:4040 в браузері - там будуть обидва URLs:
- **frontend** - порт 5173 (для замовника)
- **backend** - порт 3000 (для API)

## Крок 3: Оновіть API URL у frontend

1. Скопіюйте **backend URL** з http://localhost:4040
2. Відкрийте `frontend/src/config/api.js`
3. Тимчасово замініть:
   ```javascript
   const API_BASE_URL = 'https://ваш-backend-ngrok-url.ngrok-free.app';
   ```

## Крок 4: Перезапустіть frontend (якщо потрібно)

Якщо frontend вже запущений, просто оновіть сторінку в браузері.

Якщо потрібно перезапустити:
```bash
cd frontend
npm run dev
```

## Крок 5: Надайте URL замовнику

Скопіюйте **frontend URL** з http://localhost:4040 і надішліть замовнику!

---

**Примітка:** Якщо ви ще не автентифікували ngrok, виконайте:
```bash
ngrok config add-authtoken YOUR_TOKEN
```

