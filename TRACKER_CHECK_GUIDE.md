# 🔍 Як працює перевірка встановлення трекера

## Автоматична перевірка

Система автоматично перевіряє чи встановлено трекер на вашому сайті двома способами:

### Метод 1: Verification Ping (найнадійніший)

**Як працює:**
- Трекер автоматично надсилає verification ping кожні 5 хвилин на сервер
- Ping містить інформацію про домен та версію трекера
- Система перевіряє, чи був ping в останні 10 хвилин

**Переваги:**
- ✅ Найнадійніший метод
- ✅ Працює навіть якщо HTML не доступний
- ✅ Автоматично оновлюється кожні 5 хвилин

**Коли працює:**
- Після встановлення трекера зачекайте 5-10 хвилин
- Статус автоматично оновиться

### Метод 2: HTML Перевірка (резервний)

**Як працює:**
- Система завантажує HTML код головної сторінки вашого сайту
- Шукає індикатори наявності трекера в коді
- Перевіряє наявність tracker.js, tracker-v2.js, TRACKER_CONFIG тощо

**Переваги:**
- ✅ Працює миттєво
- ✅ Не потребує очікування

**Обмеження:**
- ⚠️ Може не працювати якщо сайт захищений
- ⚠️ Може не знайти трекер якщо він завантажується асинхронно

---

## Індикатори трекера

Система шукає такі індикатори в коді сторінки:

### Tracker v2.0 (новий):
- `tracker-v2.js`
- `_affiliateTrackerV2Initialized`
- `Affiliate Tracker v2`

### Tracker v1.0 (legacy):
- `tracker.js`
- `TRACKER_CONFIG`
- `AffiliateTracker`
- `api/track`

### Загальні індикатори:
- `TRACKER_CONFIG` - конфігурація трекера
- `window.AffiliateTracker` - об'єкт трекера
- `aff_ref_code` - ключ для зберігання реферального коду
- `api/track` - API endpoint трекера

---

## Статуси перевірки

### ✅ Підключено
Трекер знайдено на сайті. Система автоматично відстежує кліки та конверсії.

**Що це означає:**
- Трекер встановлено правильно
- Verification ping працює або HTML перевірка знайшла індикатори
- Всі функції працюють нормально

### ❌ Не підключено
Трекер не знайдено на сайті.

**Можливі причини:**
1. Код не встановлено на сайті
2. Код встановлено неправильно
3. Код встановлено не на всіх сторінках
4. Неправильний BASE_URL в конфігурації
5. Кеш браузера/CDN не очищено
6. Трекер щойно встановлено (зачекайте 5-10 хвилин для verification ping)

**Що робити:**
1. Переконайтеся, що код встановлено на **всіх** сторінках
2. Перевірте правильність `BASE_URL` в конфігурації
3. Очистіть кеш браузера та CDN
4. Зачекайте 5-10 хвилин після встановлення
5. Натисніть "Перевірити зараз" для миттєвої перевірки

---

## Швидка перевірка вручну

### В браузері:

1. Відкрийте ваш сайт
2. Натисніть F12 (відкрити DevTools)
3. Перейдіть на вкладку Console
4. Введіть: `window.AffiliateTracker`
5. Має з'явитися об'єкт трекера

**Якщо об'єкт не знайдено:**
- Трекер не встановлено або встановлено неправильно
- Перевірте чи код є на сторінці (Ctrl+U → Ctrl+F → "tracker")

### Перевірка коду на сторінці:

1. Відкрийте ваш сайт
2. Натисніть Ctrl+U (перегляд коду сторінки)
3. Натисніть Ctrl+F (пошук)
4. Шукайте: `tracker-v2.js` або `tracker.js`
5. Має знайти посилання на трекер

---

## Автоматична перевірка при додаванні сайту

Коли ви додаєте новий сайт з доменом:
1. Система автоматично перевіряє наявність трекера
2. Статус відображається одразу після додавання
3. Якщо трекер не знайдено, ви побачите інструкції що робити

---

## Часто задавані питання

### Чому статус "Не підключено" після встановлення?

**Відповідь:** Після встановлення трекера потрібно зачекати 5-10 хвилин для verification ping. 
Або натисніть "Перевірити зараз" для миттєвої HTML перевірки.

### Чому перевірка не працює для localhost?

**Відповідь:** Система не може перевірити localhost з сервера. Використайте публічний домен для перевірки.

### Як часто оновлюється статус?

**Відповідь:** 
- Verification ping - автоматично кожні 5 хвилин
- HTML перевірка - при натисканні кнопки "Перевірити зараз"
- При додаванні нового сайту - автоматично

### Що робити якщо статус неправильний?

**Відповідь:**
1. Переконайтеся, що трекер встановлено правильно
2. Перевірте правильність BASE_URL
3. Очистіть кеш браузера та CDN
4. Зачекайте 5-10 хвилин
5. Натисніть "Перевірити зараз"

---

## Технічні деталі

### Verification Ping Endpoint:
```
GET /api/track/verify?code=YOUR_CODE&domain=your-site.com&version=v2
```

### HTML Перевірка:
Система завантажує HTML сторінки та шукає індикатори трекера.

### Timeout:
HTML перевірка має timeout 10 секунд для кожного URL.

### Підтримувані версії трекера:
- ✅ Tracker v2.0 (рекомендовано)
- ✅ Tracker v1.0 (legacy)
- ✅ GTM версії обох трекерів
